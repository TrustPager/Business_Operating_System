---
description: Take a paste (CSV, screenshot text, PDF excerpt, email export, list of names) and import it as opportunities, contacts, or companies into TrustPager.
---

Run the **import-from-anywhere** skill.

Invoke the skill at `skills/import-from-anywhere/SKILL.md`. Follow its instructions exactly — including the "Important behaviours" section, which encodes safety rails that should never be skipped.
