# Connector library (fixture)

## Some Other Tool (not the widget driver)

- **What it is:** a card whose heading does NOT begin with "Widget Ads", so the
  display_name-prefix card match fails for the widget-ads driver.
- **Connect it:** unrelated.
