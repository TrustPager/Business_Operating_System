/** @type {import('next').NextConfig} */
// Lean Next.js config for the site-starter. Kept deliberately minimal so the
// starter builds standalone (Task 1.2) and Claude Design's handoff continues
// from a clean, predictable app shell. Image optimisation stays on by default
// (Core Web Vitals headroom, per web-design-method.md Part 2).
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
