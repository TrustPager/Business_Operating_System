// PostCSS pipeline for Tailwind + autoprefixer. Standard Next.js + Tailwind v3
// setup, pinned so the starter builds the same way on every machine.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
