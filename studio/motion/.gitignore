node_modules/
output/
.remotion/
*.log
# Owner footage + ingested recordings (never committed; ingest.js writes here)
public/recordings/
# Owner product screenshots for the product-demo add-on (owner-specific; Mode C)
public/screens/
# Compiled whisper.cpp + downloaded models (caption.js builds here; large, local)
whisper/
# whisper.cpp install download artifact (installWhisperCpp drops this at the root)
whisper-bin-*.zip
# Per-recording caption tracks (owner-specific; regenerated by caption.js)
data/*.captions.json
# Synthetic voiceover MP3s (owner-specific; keyed TTS, regenerated by voice.js)
public/audio/
# Per-video voice manifests (owner-specific; regenerated by voice.js)
data/*.voice.json
