/**
 * Re-export layer — source of truth is the brand-neutral UI kit (@ui).
 * Product navigation chrome (profile/service-request buttons) is intentionally
 * not carried over — a brand-agnostic primitive has no product menu.
 */
export {AvatarCircle, AvatarGroup} from '@ui/avatar';
