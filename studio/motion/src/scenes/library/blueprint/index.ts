// Blueprint / how-it-works — the schematic technical-drawing scene style.
//
// Art direction (enforced across every primitive): faint graph-paper ground with
// registration ticks, thin precise strokes that DRAW in (never bounce), mono
// uppercase labels, dimension lines + connectors doing the explaining, ONE device
// per scene, brand palette + type only. Calm spec-sheet register. If a scene
// reads as centred text, it is wrong.
export { TypographicStatement } from "./TypographicStatement";
export type { TypographicStatementProps } from "./TypographicStatement";

export { BeforeAfter } from "./BeforeAfter";
export type { BeforeAfterProps } from "./BeforeAfter";

export { ProcessFlow } from "./ProcessFlow";
export type { ProcessFlowProps, ProcessStep } from "./ProcessFlow";

export { BigStat } from "./BigStat";
export type { BigStatProps } from "./BigStat";
