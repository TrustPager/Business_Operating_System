// Clean editorial — the anchor scene style for the Content Creation Studio.
//
// Art direction (enforced across every primitive here): big type, generous
// negative space, thin ~2px strokes, ONE visual device per scene, restrained
// staggered spring entrances, brand palette + type only. Each primitive takes
// STRUCTURED props (labels, items, values) — never a paragraph of caption text.
// If a scene reads as "centred text on a background," it is wrong.
export { TypographicStatement } from "./TypographicStatement";
export type { TypographicStatementProps } from "./TypographicStatement";

export { BeforeAfter } from "./BeforeAfter";
export type { BeforeAfterProps } from "./BeforeAfter";

export { ProcessFlow } from "./ProcessFlow";
export type { ProcessFlowProps, ProcessStep } from "./ProcessFlow";

export { BigStat } from "./BigStat";
export type { BigStatProps } from "./BigStat";
