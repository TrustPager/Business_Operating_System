// Bold pop / social — the high-contrast, kinetic scene style.
//
// Art direction (enforced across every primitive): heavy uppercase sans, oversized
// scale that overfills the frame, accent-colour blocks + rotated stamps, overshoot
// slam-in motion, ONE device per scene, brand palette + type only. Built for
// thumb-stopping short-form. If a scene reads as calm or centred-quiet, it is wrong.
export { TypographicStatement } from "./TypographicStatement";
export type { TypographicStatementProps } from "./TypographicStatement";

export { BeforeAfter } from "./BeforeAfter";
export type { BeforeAfterProps } from "./BeforeAfter";

export { ProcessFlow } from "./ProcessFlow";
export type { ProcessFlowProps, ProcessStep } from "./ProcessFlow";

export { BigStat } from "./BigStat";
export type { BigStatProps } from "./BigStat";
